---
title: 发表之文章
comments: false
---

## OTReg

现有的SLM(如通过Adapter连接语音编码器和LLM)虽然在训练领域表现良好, 但是在跨数据集的时候性能有显著下降. 这是因为, 语音和文本之间存在较大的modality gap. 语音序列长并且包含非语言信息(如停顿/语速), 而文本精简且只包含语义. 模型容易过拟合语音中的非必要变化, 而非真正理解语义. 这引出下列两个挑战: 1是缺乏细粒度的语音-文本对齐监督信号, 2是动态建立的对应关系往往充满噪声, 无法有效的梯度传播.

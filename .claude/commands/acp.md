---
description: Add, commit and push current changes
---

Run the following git commands:

1. git add .
2. git commit -m "<generate commit message if not provided, coauthor: claude code>"
3. git push
